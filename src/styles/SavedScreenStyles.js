// SavedScreenStyles.js - refactored for dark mode support
import { StyleSheet } from 'react-native';

export const createSavedScreenStyles = (theme) => StyleSheet.create({
  profilecontainer: {
    flex: 1,
    backgroundColor: theme.background,
    paddingTop: 0,
    position: 'relative',
  },
  savedtitle: {
    fontSize: 22,
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 15,
    marginBottom: 15,
    color: theme.text,
  },
  savedloadingText: {
    textAlign: 'center',
    marginTop: 20,
    fontSize: 16,
    color: theme.text,
  },
  savednoData: {
    textAlign: 'center',
    marginTop: 30,
    fontSize: 16,
    color: theme.text,
  },
  savedlistContainer: {
    paddingHorizontal: 16,
    paddingBottom: 100,
  },
  savedcard: {
    backgroundColor: theme.card,
    borderRadius: 10,
    marginBottom: 30,
    overflow: 'hidden',
    elevation: 4,
    shadowColor: theme.shadow,
    shadowOpacity: 0.1,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 4,
    position: 'relative',
  },
  savedcardImage: {
    width: '100%',
    height: 200,
    backgroundColor: theme.skeleton,
  },
  savedcardContent: {
    padding: 12,
  },
  savedcardTitle: {
    fontSize: 16,
    fontWeight: 'bold',
    color: theme.text,
    marginRight: 12,
    flex: 1,
  },
  savedcardDescription: {
    fontSize: 14,
    color: theme.text,
    marginTop: 6,
    marginBottom: 6,
    flex: 1,
    marginRight: 12,
  },
  savedcardPrice: {
    fontSize: 14,
    fontWeight: 'bold',
    color: theme.primary,
    marginBottom: 8,
  },
  saveddeleteButton: {
    padding: 4,
    alignSelf: 'flex-start', // aligns within vertical flex container
  },
  guestProfileContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 32,
  },
  guestProfileText: {
    color: theme.guestBannerText,
    fontSize: 16,
    fontWeight: '500',
    textAlign: 'center',
    marginVertical: 18,
  },
  guestProfileLink: {
    color: theme.primary,
    textDecorationLine: 'underline',
    fontWeight: 'bold',
  },
  signupButton: {
    backgroundColor: theme.primary,
    paddingVertical: 12,
    paddingHorizontal: 32,
    borderRadius: 8,
    marginTop: 10,
  },
  signupButtonText: {
    color: theme.card,
    fontWeight: 'bold',
    fontSize: 16,
  },
  bottomNavWrapper: {
    position: 'absolute',
    left: 0,
    right: 0,
    bottom: 0,
    backgroundColor: theme.background,
    elevation: 10,
    shadowColor: theme.shadow,
    shadowOpacity: 0.08,
    shadowOffset: { width: 0, height: -2 },
    shadowRadius: 4,
    alignItems: 'center',
    paddingBottom: 16,
    paddingTop: 12,
  },
  emptyText: {
    fontSize: 18,
    fontWeight: '600',
    color: theme.text,
    textAlign: 'center',
    marginBottom: 8,
  },
  emptyStateContainer: {
    alignItems: 'center',
    marginTop: 50,
    paddingHorizontal: 20,
  },
  ctaButton: {
    backgroundColor: theme.primary,
    paddingVertical: 12,
    paddingHorizontal: 32,
    borderRadius: 8,
    marginTop: 18,
    alignItems: 'center',
    alignSelf: 'center',
    elevation: 2,
  },
  ctaButtonText: {
    color: theme.card,
    fontWeight: 'bold',
    fontSize: 16,
    letterSpacing: 0.5,
  },
});
