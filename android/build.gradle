// android/build.gradle
buildscript {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
  dependencies {
    // ✅ Explicit versions so Gradle can resolve them
    classpath("com.facebook.react:react-native-gradle-plugin") // <-- put YOUR RN version here
    classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    classpath("com.google.gms:google-services:4.4.2")
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url "https://jitpack.io" }
    maven { url "https://maven.revenuecat.com" }
  }
}
